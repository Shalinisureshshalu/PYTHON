n = input()

if not n.isdigit() or len(n) > 10:
    print("Invalid")
else:
    odd = 0
    even = 0

    for i in range(len(n)):
        if (i + 1) % 2 == 0:
            even += int(n[i])
        else:
            odd += int(n[i])

    print(abs(odd - even))
